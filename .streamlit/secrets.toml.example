# File esempio per Streamlit Community Cloud.
# IN LOCALE: usa il .env del repo root (load_dotenv).
# IN CLOUD: incolla questo TEMPLATE dentro
#   App → Settings → Secrets
# e compila i valori.
#
# Non committare mai il file `secrets.toml` reale.

SUPABASE_URL = "https://wicujbscimgxiprodllk.supabase.co"
SUPABASE_SERVICE_KEY = "sb_secret_..."

# Auth dashboard (vuoto = no auth — sconsigliato in cloud)
DASHBOARD_PASSWORD = "scegli-una-password-forte"

# Telegram (opzionale, se la dashboard invia notifiche manuali)
TELEGRAM_BOT_TOKEN = ""
TELEGRAM_CHAT_ID = ""
