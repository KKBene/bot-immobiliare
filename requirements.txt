# Streamlit Cloud usa questo file di default.
# Contiene SOLO le dipendenze della dashboard (build veloce ~1 min).
#
# Per il cron / dev locale usa: pip install -r requirements-bot.txt

streamlit>=1.50
plotly>=6.0
pandas>=2.3
supabase>=2.5
python-dotenv>=1.0
